# سند نقشه راه جامع ارتقای معماری، امنیت، صحت مالی و حاکمیت نسخه ۴ سامانه ERP (نسخه ۴.۰.۰ تا ۴.۰.۴۲)
> **وضعیت سند:** 📦 **سند آرشیوشده، نهایی و بسته‌شده حاکمیت نسخه ۴ (Finalized & Archived V4 Master Roadmap — v4.0.0 تا v4.0.42)**  
> **تمرکز نسخه ۴:** گذار عملگرایانه به معماری ماژولار مونولیت (Pragmatic Modular Monolith)، بستن کلیه آسیب‌پذیری‌های امنیتی شناسایی‌شده در ارزیابی جامع، صیانت اتمیک و غیرشناور از تراکنش‌های مالی و حقوقی، سنگربندی قراردادهای زمان اجرا با Zod، تفکیک گود-کامپوننت‌های فرانت‌اند، احیای پایپ‌لاین CI/CD و پایبندی کامل به اصل خلوت‌سازی (Decluttering) و تجربه کاربری بومی.  
> **پایان رسمی نسخه ۴:** شهریور ۱۴۰۵ / سپتامبر ۲۰۲۶ در نگارش v4.0.42 و انتقال حاکمیت فعال به `V5_MASTER_ROADMAP.md`.  

---

## 🎯 مبانی فلسفی و راهبردی معماری نسخه ۴

این نقشه راه، تلفیقی راهبردی میان **«اهداف ساختاری اولیه نسخه ۴»** و **«یافته‌های گزارش ارزیابی فنی مستقل»** است و تضمین می‌کند سامانه از سطح «قابل استفاده تجربی» به سطح «سامانه مالی و سازمانی شایسته اعتماد صددرصد» جهش یابد:

### ۱. اصل انضباط مالی بدون استثنا (Zero Float Tolerance & Absolute Atomicity):
هیچ محاسبه پولی در سامانه (از صدور سند حسابداری گرفته تا دستمزد پرکیسی و اضافه کارگر کارگاه) حق استفاده از محاسبات ممیز شناور (`+`, `*`, `/`) را ندارد. تمامی محاسبات پولی ملزم به استفاده انحصاری از `financialDecimal` هستند. کلیه مراحل وابسته به پول و اسناد ملزم به اجرای اتمیک در یک تراکنش دیتابیس با قفل ردیفی هستند.

### ۲. تفکیک بر مبنای مالکیت دامنه (Domain Ownership) نه نام فایل:
نام‌گذاری جداول گاهی گمراه‌کننده است؛ به عنوان مثال جدول `transactions` در واقع **دفتر کل کاردکس انبار (Stock Kardex Ledger)** است، نه تراکنش مالی! هرگونه اختلاط آن با ماژول حسابداری خطای چرخشی ایجاد می‌کند. در نسخه ۴، تفکیک اسکیما و تایپ‌ها بر اساس مالکیت بیزنس واقعی تثبیت گردیده است.

### ۳. امنیت مرز بیرونی به موازات هسته داخلی (Hardened Perimeter):
امنیت در مرکز (JWT, CSRF, Password Hashing) زمانی اثربخش است که در لایه‌های مرزی (CORS, SSRF, Metrics Exposure, PII Leakage) نشت نداشته باشد. مرزهای بیرونی و سیاست‌های دسترسی در نسخه ۴ بدون مماشات بسته می‌شوند.

### ۴. پرهیز از تله Clean Architecture افراطی (ضد انفجار بویلرپلیت):
برای سامانه‌ای با معماری ماژولار در حال رشد، رفتن به سمت الگوهای سنگین چندلایه ناکارآمد، تعداد فایل‌ها را بی‌ثمر افزایش می‌دهد. معماری هدف ما **Modular Monolith** تمیز، شفاف، با تایپ‌های دقیق و بدون لایه‌های انتزاعی زائد است.

### ۵. سنگربندی قراردادهای زمان اجرا با Zod (Runtime Contracts):
تایپ‌اسکریپت در زمان اجرا وجود ندارد. قراردادهای Zod در روت‌های حساس تضمین می‌کنند داده‌های ناقص، مقادیر منفی غیرمجاز و تزریق‌های ناخواسته هرگز به متدهای سرویس راه پیدا نکنند و داده‌های پاک‌سازی‌شده مستقیماً مصرف شوند.

### ۶. پایبندی به اصل خلوت‌سازی و تجربه کاربری آرام (Decluttering & Natural Persian):
حذف شلوغی‌های بصری، شکستن کامپوننت‌های عظیم به هوک‌ها و قطعات خوانا، حذف ترافیک شبکه‌ای اضافی ناشی از تایپ در اینپوت‌ها (Debounce) و استفاده از اصطلاحات روان، دقیق و مأنوس فارسی بدون واژگان لاتین و فنی نامناسب.

---

## 🗺️ ساختار ۷ فاز اجرایی نقشه راه نسخه ۴ (تلفیقی و ارتقایافته)

```
┌─────────────────────────────────────────────────────────────────────────────────────────────┐
│                       نقشه راه جامع نسخه ۴ (V4 Master Execution Roadmap)                    │
├─────────────────────────────────────────────────────────────────────────────────────────────┤
│ فاز ۱: سازماندهی ساختاری دامنه‌ای و رفع تضادهای نوعی دیتابیس (Foundation & Data Integrity)   │
│ فاز ۲: سنگربندی فوری امنیتی و کنترل سطح دسترسی (Immediate Security & Perimeter Hardening)  │
│ فاز ۳: صیانت از تراکنش‌های مالی، حقوق و دستمزد (Financial Precision & Transaction Guarding) │
│ فاز ۴: سنگربندی قراردادهای زمان اجرا با Zod و اعتبارسنجی ورودی‌ها (Runtime Contracts & Zod)  │
│ فاز ۵: ارتقای موتور Idempotency و مدیریت هم‌زمانی و بار (Concurrency, OCC & Scalability)   │
│ فاز ۶: خلوت‌سازی، تفکیک گود-کامپوننت‌ها و بهینه‌سازی کلاینت (Decluttering, Frontend & UX)    │
│ فاز ۷: شابلون ماژول‌های آتی، پایش پایداری و احیای CI/CD (Architecture Template, CI & Release) │
└─────────────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 📌 فاز ۱: سازماندهی ساختاری دامنه‌ای و رفع تضادهای نوعی دیتابیس (Foundation & Data Integrity)
**وضعیت:** ✅ تکمیل‌شده (۱۰۰٪ — زیرفازهای ۱.۱ تا ۱.۵ — v4.0.8)  
**تمرکز:** حفظ سازگاری ۱۰۰٪ رو به عقب، تفکیک دامنه‌ای و رفع دروغ‌های نوعی در لایه ORM

### زیرفاز ۱.۱: تفکیک اسکیما بر پایه ماتریس مالکیت واقعی (`src/db/schema/`) [انجام‌شده ✅]
* تفکیک فایل یکپارچه `schema.ts` به ۱۲ ماژول مستقل: `auth.ts`, `settings.ts`, `inventory.ts`, `crm.ts`, `projects.ts`, `documents.ts`, `accounting.ts`, `personnel.ts`, `dailyLogs.ts`, `procurement.ts`, `workflow.ts`, `events.ts`.
* حفظ نقطه ورود مرکزی `src/db/schema.ts` با اکسپورت فراگیر جهت تضمین صفر شکستگی در تمامی سرویس‌ها.

### زیرفاز ۱.۲: تفکیک دامنه‌ای تایپ‌های سامانه (`src/types/`) [انجام‌شده ✅]
* تفکیک تایپ‌های کلان پروژه به ماژول‌های موضوعی: `auth.types.ts`, `inventory.types.ts`, `accounting.types.ts`, `crm.types.ts`, `project.types.ts`, `dailyLogs.types.ts`, `personnel.types.ts`, `procurement.types.ts`, `common.types.ts`.
* اکسپورت مرکزی از طریق `src/types/index.ts` و فایل ریشه `src/types.ts`.

### زیرفاز ۱.۳: تدوین سند قوانین صلب ده‌گانه معماری (`ARCHITECTURE_RULES.md`) [انجام‌شده ✅]
* تدوین قوانین شماره‌دار، صلب و شفاف برای تضمین پایداری معماری و هدایت ایجنت‌های هوش مصنوعی.

### زیرفاز ۱.۴: رفع تضاد نوعی ستون‌های مالی دیتابیس (یافته D-1 / بدهی TD-094) [انجام‌شده ✅]
* **اقدام انجام‌شده (v4.0.7):** استانداردسازی کامل تمامی ستون‌های مالی و مقداری در کلیه فایل‌های اسکیما (`accounting.ts`, `inventory.ts`, `procurement.ts`, `personnel.ts`, `projects.ts`, `documents.ts`, `crm.ts`, `dailyLogs.ts`) با پیکربندی بومی `{ mode: 'number' }` Drizzle ORM، جایگزینی کست‌های زائد و اصلاح انتساب‌های روت‌های ووکامرس بدون کوچکترین افت کارایی یا شکستگی تایپ.

### زیرفاز ۱.۵: ارتقای یکپارچگی لاگ‌های ممیزی و سیاست نگه‌داشت (یافته D-2) [انجام‌شده ✅]
* **اقدام انجام‌شده (v4.0.8):** 
  1. بازمهندسی تابع `purgeOldAuditLogs` با اعمال کف نگه‌داشت ۹۰ روزه و ممانعت از پاکسازی بی‌رویه سوابق ممیزی.
  2. محافظت خودکار از لاگ‌های حساس (حذف‌ها `DELETE`، تغییر دسترسی‌ها `PERMISSION`/`ROLE`، تغییرات تنظیمات سیستم و احراز هویت) حتی با انتخاب دوره‌های نگه‌داشت قدیمی‌تر.
  3. ارائه اندپوینت‌های اختصاصی `POST /api/system/activity-logs/purge` و `GET /api/system/activity-logs/integrity` و تعبیه کارت مدیریتی لاگ‌ها در زبانه عملیات سیستم همراه با بج‌های بصری عملیات‌های جدید.
  4. اعتبارسنجی کامل با آزمون رگرسیون `v4_audit_log_retention_and_integrity_guard` در سوئیت تست سیستم.

---

## 📌 فاز ۲: سنگربندی فوری امنیتی و کنترل سطح دسترسی (Immediate Security & Perimeter Hardening)
**وضعیت:** ✅ تکمیل‌شده (۱۰۰٪ — زیرفازهای ۲.۱، ۲.۲، ۲.۳، ۲.۴ و ۲.۵ — v4.0.23)  
**تمرکز:** مسدودسازی روزنه‌های مرزی پروداکشن، رفع بای‌پس‌های شبکه، پاک‌سازی مقادیر پیش‌فرض و حفظ حریم خصوصی داده‌های بانکی

### زیرفاز ۲.۱: مسدودسازی حفره CORS با Credentials در پروداکشن (یافته S-1) [انجام‌شده ✅]
* **ریشه مسئله:** در `src/app.ts` رجکس `isCloudRunOrAiStudio` قبل از شرط پروداکشن به تمامی دامنه‌های `*.run.app` و `googleusercontent.com` با کوکی `credentials: true` اجازه دسترسی می‌داد.
* **اقدام انجام‌شده (v4.0.20):**
  1. طراحی و استخراج ماژول متمرکز و مستقل `src/lib/corsValidator.ts` جهت اعتبارسنجی قطعی، تست‌پذیر و ایزوله مبداهای CORS.
  2. گیت‌گذاری صلب دامنه‌های توسعه: دامنه‌های توسعه، پیش‌نمایش Cloud Run (`*.run.app`) و لوکال‌هاست منحصراً در صورت `NODE_ENV !== 'production'` مجاز هستند و در محیط عملیاتی به صورت قطعی مسدود شدند.
  3. الزام فهرست سفید صریح `ALLOWED_ORIGINS` و تطابق دامنه معتبر `APP_URL` در پروداکشن همراه با لاگ‌های هشدار امنیتی استاندارد.
  4. ارتقای تست‌های امنیتی سیستم در `securitySuite.ts` از منطق شبیه‌سازی به اجرای مستقیم کد واقعی (unit test) و افزودن آزمون‌های اعتبارسنجی انسداد قطعی S-1 و حل بدهی فنی TD-088.

### زیرفاز ۲.۲: تقویت و اصلاح قطعی گارد ضد SSRF (یافته S-2) [انجام‌شده ✅]
* **ریشه مسئله:** تابع `ssrfGuard.ts` در برابر آدرس‌های IPv4-mapped IPv6 (مانند `::ffff:169.254.169.254` یا `::ffff:127.0.0.1`) دور می‌خورد و امکان دسترسی به متادیتای سرور ابری یا لوپ‌بک را مهیا می‌ساخت.
* **اقدام انجام‌شده (v4.0.21):**
  1. پیاده‌سازی متد تجزیه و بسط عمیق آدرس‌های IPv6 (`expandIPv6`) به آرایه کلمات ۱۶ بیتی با پشتیبانی از فشرده‌سازی `::` و پسوندهای نقطه‌دار IPv4.
  2. اعتبارسنجی اتمیک آدرس‌های ترکیبی IPv4-mapped IPv6 (`::ffff:0:0/96`) و استخراج بایت‌های IPv4 جهت اعمال ضوابط شبکه خصوصی.
  3. پوشش جامع بازه‌های Carrier-Grade NAT (RFC 6598: `100.64.0.0/10`)، ULA (`fc00::/7`)، Link-Local (`fe80::/10`)، مالتی‌کست و آدرس‌های متادیتای کلود (`169.254.169.254`).
  4. حذف براکت‌ها (`[...]`) و نرمال‌سازی کامل هاست‌نیم در `assertSafeExternalUrl` قبل از تفکیک DNS و ارزیابی مسدودسازی.
  5. بازطراحی آزمون امنیتی `sec_ssrf_protection_guard` به اجرای کد واقعی (`real_code`) و راستی‌آزمایی کلیه سناریوهای حمله و بای‌پس. حل قطعی بدهی فنی TD-089.

### زیرفاز ۲.۳: احراز هویت و اعتبارسنجی واقعی توکن در روت‌های متریک (یافته S-3) [انجام‌شده ✅]
* **ریشه مسئله:** اندپوینت‌های `/metrics` و `/api/metrics` صرفاً وجود یک هدر Bearer دلخواه را چک می‌کنند بدون اینکه توکن JWT اعتبارسنجی شود.
* **اقدام انجام‌شده (v4.0.22):**
  1. طراحی و استقرار میدل‌ور مستقل و ایمن `metricsAuthMiddleware` در `src/middleware/metricsAuth.ts`.
  2. اعتبارسنجی قطعی توکن‌های ارسالی با امضای کریپتوگرافیک `jwt.verify` و الزام بررسی نقش مدیر (`role === 'admin'`)، و مسدودسازی دسترسی کاربران عادی یا متفرقه با ۴۰۳ Forbidden.
  3. پشتیبانی ایمن از اسکرپرهای پرومتئوس با توکن اختصاصی `process.env.METRICS_TOKEN` از طریق مقایسه در زمان ثابت (`crypto.timingSafeEqual`) با تابع `safeCompareTokens` جهت پیشگیری از حملات کانال جانبی زمان‌بندی (Timing Attacks).
  4. مسدودسازی کامل توکن‌های رندوم و امضانشده دلخواه (یافته S-3) با خطای قطعی ۴۰۱ Unauthorized.
  5. پیاده‌سازی آزمون جامع امنیتی `sec_metrics_authentication_guard` با اجرای کد واقعی (`real_code`) و اعتبارسنجی تمامی سناریوهای دسترسی معتبر و متخاصم در `securitySuite.ts`. حل بخش اول بدهی فنی TD-092.

### زیرفاز ۲.۴: پاک‌سازی سکرت‌ها و مقادیر پیش‌فرض هاردکد (یافته S-4) [انجام‌شده ✅]
* **ریشه مسئله:** استفاده از کلید‌های فال‌بک پیش‌فرض در توابع JWT و امکان اجرای روت‌های ستاپ یا اسکریپت‌های اولیه بدون تاییدیه صریح توکن امنیتی در محیط‌های عملیاتی.
* **اقدام انجام‌شده (v4.0.22):** 
  1. تبدیل کلید JWT فال‌بک توسعه در `src/middleware/auth.ts` و `getJwtSecret` به خطای قطعی در صورت فقدان یا استفاده از کلیدهای پیش‌فرض و ناامن (`INSECURE_DEFAULT_SECRETS`) در محیط پروداکشن.
  2. الزام متغیر `ERP_SETUP_TOKEN` در روت‌های ستاپ وب (`/api/setup`) با حداقل طول ۱۶ کاراکتر، جلوگیری از race condition با قفل ادوایزری (`pg_try_advisory_lock(79234)`) و ممانعت از تعریف پسورد پیش‌فرض `admin123456` در پروداکشن.
  3. افزودن گارد صلب ممانعت از اجرای اسکریپت `scripts/initial-setup.ts` در پروداکشن بدون پارامتر `--setup-token` و حذف کامل امکان استفاده از پسوردهای ضعیف.
  4. استقرار تست امنیتی `sec_setup_token_and_advisory_lock` در `securitySuite.ts`. حل کامل بدهی فنی TD-092.

### زیرفاز ۲.۵: صیانت از اطلاعات حساس بانکی پرسنل و مسدودسازی نشت PII (یافته S-5 / TD-090) [انجام‌شده ✅]
* **ریشه مسئله:** در روت‌های `piecework.routes.ts` و `personnel.routes.ts` فیش‌های حقوقی و رکوردهای پرسنل اطلاعات حساس بانکی شامل شماره کارت ۱۶ رقمی، شماره شبا، شماره حساب، نام بانک، و نام کاربری/رمز صرافی نوبیتکس را بدون گارد مجوز اختصاصی به کاربران احراز هویت شده بازمی‌گرداندند.
* **اقدام انجام‌شده (v4.0.23):**
  1. طراحی و استقرار ماژول اختصاصی `src/lib/piiMasker.ts` با توابع استاندارد ماسک‌سازی `maskCardNumber`، `maskShebaNumber`، `maskAccountNumber` و `maskNobitexUsername` طبق ضوابط شبکه شتاب و بانکداری الکترونیک ایران.
  2. پیاده‌سازی متد اعتبارسنجی سطح دسترسی داده‌های مالی حساس `canAccessSensitivePersonnelData` جهت تفکیک دسترسی مدیران، دارندگان مجوزهای مالی صریح (`payroll.view_sensitive`، `personnel.manage` و ...) و دسترسی پرسنل به رکوردهای متعلق به خود.
  3. اعمال ماسک‌سازی و پالایش خودکار در تمام روت‌های لیست و جزئیات پرسنل (`GET /api/personnel`، `GET /api/personnel/:id`) و حفاظت از روت خروجی اکسل پرسنل (`GET /api/personnel/export`) با گارد صلب `authorize`.
  4. اعمال ماسک‌سازی در تمامی روت‌های فیش‌های دستمزد پرکیسی (`GET /api/piecework/payrolls`، `GET /api/piecework/payrolls/mine` و `GET /api/piecework/payrolls/:id`) به همراه حذف قطعی پسوردهای صرافی.
  5. استقرار آزمون امنیتی جامع `sec_personnel_pii_masking_and_guard` در `securitySuite.ts` و راستی‌آزمایی تمام سناریوها با اجرای کد واقعی (`real_code`). حل قطعی بدهی فنی بحرانی **TD-090**.

---

## 📌 فاز ۳: صیانت از تراکنش‌های مالی، حقوق و دستمزد (Financial Precision & Transaction Guarding)
**وضعیت:** ✅ تکمیل‌شده (۱۰۰٪ — زیرفازهای ۳.۱، ۳.۲، ۳.۳ و ۳.۴ — v4.0.6)  
**تمرکز:** پایان دادن به محاسبات اعشاری شناور، تضمین اتمیک بودن صدور فیش و رفع خطاهای بلعیده‌شده

### زیرفاز ۳.۱: تراکنشی‌کردن کامل چرخه صدور فیش حقوقی (یافته F-2) — ✅ تکمیل‌شده (v4.0.4)
* **ریشه مسئله:** تولید فیش حقوقی در ۳ کوئری مستقل خارج از تراکنش انجام می‌شود؛ اگر صدور سند شکست بخورد، فیش بدون ثبت حسابداری باقی می‌ماند و در تولید هم‌زمان ریسک پرداخت مجدد وجود دارد. همچنین شماره فیش با الگوی ریس‌دار `count(*)+1001` محاسبه می‌شود.
* **اقدام اجرایی انجام‌شده:** 
  1. قرار دادن کل مراحل (ثبت فیش، قفل ردیفی پرسنل و لاگ‌ها، اتصال لاگ‌ها با گارد `WHERE payrollId IS NULL` و ثبت سند دوبل) در یک تراکنش واحد `orm.transaction`.
  2. استفاده از قفل ردیفی صلب `.for('update')` روی لاگ‌های کارکرد واکشی‌شده و رکورد پرسنل.
  3. تخصیص شماره فیش از طریق شمارنده و توالی اتمیک `piecework_payroll_number_seq` در PostgreSQL بدون ریسک هم‌زمانی.
  4. اعتبارسنجی با آزمون همروندی `conc_piecework_payroll_atomic_race_guard` در سوئیت تست سیستم.

### زیرفاز ۳.۲: حذف کامل محاسبات شناور در دستمزد کارمزدی (یافته F-1) — ✅ تکمیل‌شده (v4.0.4)
* **ریشه مسئله:** در `piecework.routes.ts` و سرویس پرداخت حقوق، ضرب و جمع مبالغ با اپراتورهای شناور جاوااسکریپت (`*`, `+`) انجام می‌شود که مغایر با استاندارد داخلی `financialDecimal` است.
* **اقدام اجرایی انجام‌شده:** بازنویسی تمامی محاسبات محاسبه دستمزد ناخالص، پاداش‌ها، بیمه، مساعده و مانده نهایی فیش در `POST /api/piecework/payrolls` با کلاس دقیق `FinancialDecimal` و تضمین انضباط مالی ممیز ثابت.

### زیرفاز ۳.۳: حذف رویه بلعیده‌شدن خطاهای اسناد مالی و ممیزی (یافته F-3) — ✅ تکمیل‌شده (v4.0.5)
* **ریشه مسئله:** شکست صدور سند اتوماتیک حسابداری در فیش حقوقی و افتتاح حساب بانکی با `logger.warn` نادیده گرفته می‌شود و تراکنش اصلی را موفق اعلام می‌کند.
* **اقدام اجرایی انجام‌شده:** 
  1. پیاده‌سازی گارد اعتبارسنجی صلب (`strict` mode) در تمامی متدهای صدور سند خودکار حسابداری در `VoucherSyncService` (فروش، خرید، رسید تولید، حواله انبار و حقوق و دستمزد).
  2. پرتاب خطاهای صلب `ValidationError` و `NotFoundError` به جای بازگرداندن `null` در صورت نقص نگاشت کدینگ حسابداری یا شکست صدور سند.
  3. تراکنشی‌کردن کامل و اتمیک تعریف حساب‌های بانکی و صندوق‌ها در `BankAccountService` درون `orm.transaction` به همراه صدور تضمینی سند افتتاحیه و اصلاح فرمول سند تعدیل مانده.
  4. فعال‌سازی حالت صلب در روت‌های صدور فیش و تریگرهای دستی صدور سند حسابداری.
  5. اعتبارسنجی با سناریوی ممیزی `v4_strict_financial_voucher_guard` در سوئیت تست سیستم و حل قطعی بدهی فنی **TD-093**.

### زیرفاز ۳.۴: ارکستراسیون اتمیک اسناد و فاکتورها (Document Orchestration) — ✅ تکمیل‌شده (v4.0.6)
* **ریشه مسئله:** فرآیند نهایی‌سازی اسناد انبار و فاکتورها نیازمند تفکیک شفاف، ترتیبی و کامپوزپذیر گام‌های عملیاتی بود تا از وقوع هرگونه ناسازگاری یا گردش انبار ناقص قبل از اعمال حسابداری و ارسال ایونت‌های دامنه جلوگیری شود.
* **اقدام اجرایی انجام‌شده:** 
  1. پیاده‌سازی ارکستراسیون صلب ۳ مرحله‌ای در `finalizeDocument` و `createDocument` (گام ۱: اعتبارسنجی پیش‌پرواز، قفل سطری ردیف با `.for('update')` و مسدودسازی اسناد با صفر قلم کالا؛ گام ۲: اعمال گردش انبار و اصلاح کاردکس با حفظ میانگین موزون قیمت WAC و کنترل سیاست کسری منفی؛ گام ۳: به‌روزرسانی وضعیت سند به `final`، ثبت ایونت دامنه در Outbox تراکنشی و صدور هم‌زمان سند دوبل حسابداری با ارسال صلب `tx` طبق قاعده **DB-008**).
  2. پشتیبانی کامل از `externalTx?: DbExecutor` در متدهای `finalizeDocument` و `deleteDocument` جهت مشارکت شفاف در تراکنش‌های خارجی بزرگتر و کامپوزپذیری خط لوله‌ها.
  3. اعتبارسنجی با سناریوی ممیزی `v4_atomic_document_orchestration` در سوئیت تست سیستم.

---

## 📌 فاز ۴: سنگربندی قراردادهای زمان اجرا با Zod و اعتبارسنجی ورودی‌ها (Runtime Contracts & Zod)
**وضعیت:** ✅ تکمیل‌شده (۱۰۰٪ — زیرفازهای ۴.۱، ۴.۲، ۴.۳ و ۴.۴ — v4.0.12)  
**تمرکز:** مسدودسازی ورودی‌های خام و اعمال ولیدیشن صلب بر کلیه مسیرهای جهش داده

### زیرفاز ۴.۱: اصلاح بنیادین میدل‌ور ولیدیشن (یافته S-6) ✅ (تکمیل شده در v4.0.9)
* **ریشه مسئله:** میدل‌ور `src/middleware/validate.ts` داده‌ها را اعتبارسنجی می‌کند اما نتیجه تمیز و فیلترشده Zod را به `req.body` جایگزین نمی‌کند؛ همچنین برخی عملیات انبار پشت مجوز view قرار دارند.
* **اقدام اجرایی:**
  1. الصاق مستقیم خروجی `parsed` شده Zod به `req.body`، `req.query` و `req.params` جهت جلوگیری از مصرف داده‌های آلوده یا ناخواسته.
  2. ارتقای پرمیشن‌های تغییر و آزادسازی تخصیص‌های انبار (`/allocations/:id/release`) به سطوح مدیریتی (`inventory.reconcile`, `projects.edit`, `warehouse.manage`).
  3. اعتبارسنجی با سناریوی ممیزی `v4_runtime_validation_middleware_guard` در سوئیت تست‌های امنیتی سیستم.

### زیرفاز ۴.۲: قراردادهای ورودی اسناد و فاکتورها (Documents Contracts) ✅ (تکمیل شده در v4.0.10)
* **ریشه مسئله:** احتمال ارسال مقادیر منفی در تخفیف یا قیمت اقلام فاکتورها و عدم وجود اعتبارسنجی صلب بر روی فیلترها و مسیرهای واکشی اسناد.
* **اقدام اجرایی:**
  1. تدوین شِماهای صلب `documentItemInputSchema`، `documentCreateSchema`، `documentUpdateSchema` و اعتبارسنجی مقادیر مثبت اقلام و نامنفی بودن قیمت و تخفیف.
  2. تجهیز تمامی روت‌های خواندن، انبارگردانی و تطبیق موجودی (`GET /documents`, `GET /documents/audit-items`, `GET /documents/:id/settlement-status`, `POST /documents/reconcile-stock`) به میدل‌ور `validate`.
  3. اعتبارسنجی جامع با سناریوی `v4_document_runtime_contracts_guard` در سوئیت تست‌های یکپارچگی اسناد سیستم.

### زیرفاز ۴.۳: قراردادهای ورودی کاردکس و انبارداری (Inventory Contracts) ✅ (تکمیل شده در v4.0.11)
* **ریشه مسئله:** احتمال ثبت حواله با مبدأ و مقصد یکسان، ارسال مقادیر منفی در انتقالات یا تخصیص‌های BOM پروژه و مقادیر نامعتبر سیاست موجودی منفی.
* **اقدام اجرایی انجام‌شده:**
  1. تدوین شِماهای صلب `transferStockSchema`، `negativeStockPolicySchema`، `projectAllocateSchema`، `projectReceiptAllocateSchema`، `releaseAllocationSchema` و `allocationsQuerySchema`.
  2. مسدودسازی صلب انتقال به همان انبار (Self-Transfer Block) و الزام مقادیر مثبت برای اقلام انتقال و تخصیص در لایه شِما.
  3. تجهیز کلیه روت‌های انبارداری، ممیزی، بازسازی کاردکس و تخصیص به میدل‌ور `validate`.
  4. نگارش سناریوی اختصاصی `v4_inventory_runtime_contracts_guard` در سوئیت تست‌های رگرسیون سیستم و تایید صددرصدی آزمون‌ها.

### زیرفاز ۴.۴: قراردادهای اسناد دوبل و خزانه‌داری (Accounting & Treasury Contracts) ✅ (تکمیل شده در v4.0.12)
* **ریشه مسئله:** عدم الزام تراز بودن بدهکار و بستانکار در لایه ورود داده، ریسک انتقال وجه به همان حساب بانکی، ثبت چک با فرمت نامعتبر صیادی و پارامترهای کوئری کنترل‌نشده در گزارشات مالی.
* **اقدام اجرایی انجام‌شده:**
  1. تدوین شِماهای صلب `createVoucherSchema`, `updateVoucherSchema`, `correctVoucherSchema`, `repostVoucherSchema` با گارد `refine` جهت تراز قطعی مجموع مبالغ بدهکار و بستانکار (`Debit == Credit`) و نامنفی بودن مقادیر.
  2. تدوین شِمای `createChequeSchema` با اعتبارسنجی فرمت ۱۶ رقمی شناسه صیادی و کنترل‌های چک دریافتی/پرداختی.
  3. تدوین شِمای `transferSchema` خزانه‌داری با ممانعت قاطع از حساب‌های مبدا و مقصد یکسان.
  4. تجهیز کلیه روت‌های گزارشات مالی (تراز آزمایشی، ترازنامه، سود و زیان، دفتر روزنامه، کارت حساب) به شِماهای فیلتر کوئری Zod.
  5. الصاق میدل‌ور `validate` به تمامی ۳۴ روت ماژول حسابداری و خزانه‌داری در `src/routes/accounting.routes.ts`.
  6. نگارش سناریوی اختصاصی `v4_accounting_treasury_runtime_contracts_guard` در سوئیت تست‌های رگرسیون سیستم و تایید صددرصدی آزمون‌ها.

---

## 📌 فاز ۵: ارتقای موتور Idempotency و مدیریت هم‌زمانی و بار (Concurrency, OCC & Scalability)
**وضعیت:** ✅ تکمیل‌شده (در نگارش‌های v4.0.13 تا v4.0.15)  
**تمرکز:** ایمن‌سازی کلیدهای ایدمپوتنسی، رفع گلوگاه‌های پردازشی، حذف N+1 و فعال‌سازی قفل‌های ترتیبی

### زیرفاز ۵.۱: ارتقای ساختار کلید و دامنه موتور Idempotency (یافته F-4) ✅ (تکمیل شده در v4.0.13)
* **ریشه مسئله:** کلید ایدمپوتنسی صرفاً روی ستون `key` یکتا است که خطر نشت داده میان کاربران دارد و فقط روی ۴ روت از ۳۴۶ روت فعال است.
* **اقدام اجرایی انجام‌شده:** 
  1. ارتقای جدول و قید یکتایی به ترکیب سه‌گانه `(userId, scope, key)` در مایگریشن اتمیک ۸.
  2. گسترش پوشش میدل‌ور به کلیه مسیرهای ثبت سند، پرداخت حقوق، ثبت چک، جابجایی انبار و اسناد دوبل حسابداری.
  3. اعمال سیاست fail-closed برای درخواست‌های بدون هدر در روت‌های حساس مالی و راستی‌آزمایی با تست اختصاصی همروندی.

### زیرفاز ۵.۲: تعیین‌تکلیف و فعال‌سازی ماشین‌آلات هم‌زمانی (یافته A-1) ✅ (تکمیل شده در v4.0.14)
* **ریشه مسئله:** ابزارهای مدیریت هم‌زمانی نظیر `withOrderedLocks` و `withOccRetry` به صورت کدهای یتیم و غیرمتصل به مسیرهای حساس تراکنشی وجود داشتند و ریسک ایجاد بن‌بست (Deadlock) در تراکنش‌های متداخل چندجدولی وجود داشت.
* **اقدام اجرایی انجام‌شده:**
  1. ارتقا و بازطراحی کامل `withOrderedLocks` در `src/lib/lockOrder.ts` با پشتیبانی از نمونه‌های جدول Drizzle (`getTableName`)، مرتب‌سازی صعودی کلیدها و اعتبارسنجی اولویت قفل‌ها.
  2. تدوین هلپر جامع `executeOccUpdate` در `src/lib/occHelper.ts` برای اجرای امن چرخه واکشی، اعتبارسنجی نسخه و تلاش مجدد با جیتر.
  3. اتصال رسمی `withOrderedLocks` به سرویس تخصیص مواد و قطعات پروژه (`projectBomAllocation.service.ts`)، پرداخت حقوق خزانه‌داری (`payrollPayment.service.ts`)، انتقال بین‌انباری (`inventoryStockRepair.service.ts`) و روت‌های مدیریت پروژه (`projects.routes.ts`).
  4. افزودن آزمون‌های اختصاصی بافت قفل‌های ترتیبی و سناریوی OCC در سوئیت تست‌های همروندی `concurrencySuite.ts` و پاس شدن ۱۰۰٪ آزمون‌ها (۱۳ از ۱۳).

### زیرفاز ۵.۳: رفع گلوگاه‌های کوئری و N+1 سرور (یافته A-2) ✅ (تکمیل شده در v4.0.15)
* **ریشه مسئله:** استعلام مکرر کاربر از دیتابیس در هر ریکوئست احرازشده، حلقه‌های کوئری N+1 در واکشی تعاریف و تسک‌های گردش‌کار، فراخوانی کامل تسک‌ها در `getTaskStats`، روت‌های بدون محدودیت مانند `/transfers` و واکشی‌های منفرد در رزروهای پروژه.
* **اقدام اجرایی انجام‌شده:**
  1. پیاده‌سازی کش سبک درون‌حافظه‌ای با TTL ۳۰ ثانیه در میدل‌ور `authenticateToken` همراه با متد ابطال فوری `invalidateUserAuthCache`.
  2. رفع کوئری‌های N+1 در `WorkflowDefinitionService.getDefinitions` با تجمیع شناسه‌ها و بارگذاری موازی با `Promise.all`.
  3. بازنویسی متد `WorkflowTaskService.getTaskStats` با کوئری‌های مستقیم شمارش در دیتابیس (`count()`) و حذف کامل فراخوانی سربار `getMyTasks`.
  4. بهینه‌سازی `WorkflowTaskService.getMyTasks` جهت بارگذاری داده‌های کانتکست منحصراً برای ردیف‌های صفحه جاری.
  5. افزودن صفحه‌بندی استاندارد (`page`, `limit`, `total`, `totalPages`) و فیلترهای جستجو به روت `/api/transfers`.
  6. بهینه‌سازی واکشی اقلام رزرو پروژه در `documents.routes.ts` با عملگر دسته‌ای `inArray`.
  7. ثبت آزمون‌های رگرسیون تایید کش احراز هویت، صفحه‌بندی و کوئری‌های گردش‌کار با موفقیت ۱۰۰٪.

---

## 📌 فاز ۶: خلوت‌سازی، تفکیک گود-کامپوننت‌ها و بهینه‌سازی کلاینت (Decluttering, Frontend & UX)
**وضعیت:** ⏳ در حال اجرا  
**تمرکز:** شکستن کامپوننت‌های حجیم، حذف ترافیک سرگردان شبکه و اجرای اصل سادگی

### زیرفاز ۶.۱: شکستن گود-کامپوننت‌های فرانت‌اند و انتقال محاسبات از رندر (یافته Q-1) ✅ (تکمیل شده در v4.0.16)
* **ریشه مسئله:** فایل‌هایی نظیر `BankAndTreasuryTab.tsx` (۱,۸۷۴ خط) محاسبات مانده دوبل حسابداری را در بدنه رندر انجام می‌دهند و استیت‌های سنگین را مدیریت می‌کنند.
* **اقدام اجرایی انجام‌شده:**
  1. استخراج کاستوم هوک اختصاصی `useTreasuryCalculations` در `src/components/accounting/treasury/useTreasuryCalculations.ts` جهت مدیریت محاسبات مالی، خلاصه‌های آماری، فیلترهای چندگانه و مانده در لحظه با استفاده از `useMemo`.
  2. تفکیک و شکستن گود-کامپوننت `BankAndTreasuryTab.tsx` به ۹ زیرکامپوننت تخصصی و سبک:
     - `TreasuryHealthBanner.tsx`: بنر سلامت و انطباق اسناد دوبل با خزانه‌داری
     - `BankAccountCard.tsx`: کارت اطلاعات حساب، شبا، کارتخوان و وضعیت آنلاین
     - `BankAccountGrid.tsx`: شبکه نمایش حساب‌ها و کارت اقدام تعریف حساب جدید
     - `TreasuryTransactionsTable.tsx`: جدول تاریخچه و گردش اسناد خزانه با فیلترها، جستجو و خروجی اکسل
     - `BankAccountModal.tsx`: مودال تعریف و ویرایش حساب‌های بانکی، صندوق و تنخواه‌گردان
     - `TreasuryTransactionModal.tsx`: مودال ثبت رسید دریافت و پرداخت با پیش‌نمایش سند دوبل و آپلود پیوست
     - `TreasuryTransferModal.tsx`: مودال انتقال وجه بین حساب‌ها با صدور سند متقابل
     - `TreasuryVoidModal.tsx`: مودال ابطال سند با ثبت سند معکوس خودکار
     - `CashFlowModal.tsx`: مودال صورت گردش نقدینگی و تحلیل ورودی/خروجی وجوه
  3. کاهش ۷۹ درصدی حجم کد کامپوننت اصلی `BankAndTreasuryTab.tsx` از ۱,۸۷۵ خط به ۳۹۸ خط و پاس شدن کلیه آزمون‌های بیلد و تایپ‌اسکریپت.

### زیرفاز ۶.۲: پیاده‌سازی Debounce یکپارچه در جستجو و بهینه‌سازی رندر (یافته U-1) — ✅ تکمیل‌شده (v4.0.17)
* **ریشه مسئله:** جستجو در لیست اسناد به ازای هر فشردن کلید، درخواست سرور ارسال می‌کرد و جداول بزرگ بدون دیبانس با هر کی‌استروک رندر و محاسبه مجدد می‌شدند؛ همچنین سلکتورهای آنلاین در معرض شرایط رقابتی (Race Condition) قرار داشتند.
* **اقدام اجرایی انجام‌شده:** 
  1. پیاده‌سازی هوک‌های جنریک `useDebounce` و `useDebouncedCallback` در `src/hooks/useDebounce.ts`.
  2. گسترش `SearchContext` با افزودن `debouncedSearchQuery` (تاخیر ۳۵۰ میلی‌ثانیه) و متد صلب `clearSearch` جهت پاک‌سازی سریع فیلترها در کنار حفظ تجربه روان تایپ در اینپوت.
  3. تجهیز کامپوننت `SearchableSelect` به `AbortController` جهت لغو درخواست‌های قدیمی و تاخیر ۳۰۰ میلی‌ثانیه در جستجوی آنلاین جهت ممانعت قطعی از Race Condition.
  4. مهاجرت کامل صفحات `InvoicesListPage`، `ItemsPage`، `CustomersPage` و `TransactionsPage` به تغذیه کوئری‌های واکشی سرور با `debouncedSearchQuery` و ریست صفحه بر اساس مقدار دیبانس‌شده.
  5. بهینه‌سازی فیلتر پروژه‌ها در `ProjectsPage` و فیلتر اقلام در `PricingPage` با بهره‌گیری از `useMemo` و مقدار دیبانس‌شده جستجو.
  6. اتصال محاسبات مالی لحظه‌ای خزانه‌داری در `BankAndTreasuryTab` به `debouncedSearchQuery` و حفظ سرعت پاسخ‌دهی تایپ در اینپوت جستجو.

### زیرفاز ۶.۳: کاهش تکرار کدهای رابط کاربری و ایجاد ماژول‌های پایه (یافته Q-2) — ✅ تکمیل‌شده (v4.0.18)
* **ریشه مسئله:** پراکندگی و تکرار چند ده‌باره کد اسکلت مودال‌ها در سراسر فرانت‌اند، کدهای تکراری چاپ و نشتی استایل پنجره پرینت به کل صفحه، و فراخوانی‌های مکرر و بدون کش شبکه‌ای برای پر کردن سلکتورهای اشخاص و تامین‌کنندگان در کامپوننت‌های مختلف.
* **اقدام اجرایی انجام‌شده:**
  1. طراحی و استخراج کامپوننت پایه `Modal` در `src/components/common/Modal.tsx` با انیمیشن روان، مدیریت کلید Escape، قفل هوشمند اسکرول بدنه، فوتر اکشن ساختاریافته و شناسه‌های دسترسی‌پذیری.
  2. ایجاد ماژول یکپارچه چاپ `PrintModal` و هلپر متمرکز `src/utils/printHelper.ts` (`executePrint` و `useDocumentPrint`) جهت مدیریت ایمن کلاس `printing-doc` و حل مشکل پرینت رابط کاربری پس‌زمینه.
  3. توسعه هوک‌های کاستوم `useEntitySelectors.ts` (`useCustomerSelectOptions`, `useSupplierSelectOptions`, `usePersonnelSelectOptions`) با بهره‌گیری از قابلیت کش ۵ دقیقه‌ای `react-query` و حذف درخواست‌های زائد تکراری شبکه.
  4. مهاجرت مودال‌های سامانه نظیر `ConfirmModal` و `VoucherReversalModal` به کامپوننت استاندارد `Modal`.
  5. مهاجرت مودال‌های پیش‌نمایش چاپ `PrintDocModal` و `VoucherPrintModal` به `PrintModal` استاندارد.
  6. یکپارچه‌سازی `SplitOrderModal` و `CreatePurchaseOrderModal` با هوک متمرکز `useSupplierSelectOptions`.
  7. ایجاد نقطه خروجی مرکزی `src/components/common/index.ts` جهت سهولت توسعه‌های آتی.

### زیرفاز ۶.۴: ارتقای دسترس‌پذیری، مدیریت خطای بصری و زبان فارسی روان — ✅ تکمیل‌شده (v4.0.19)
* **ریشه مسئله:** احتمال کرش سراسری صفحه در اثر خطای زمان اجرای یک ساب‌کامپوننت یا تب فرعی (بروز پدیده صفحه سفید / White Screen)، عدم وجود نمای خطای کاربرپسند با امکان تلاش مجدد، و وجود برچسب‌های فنی لاتین در رابط کاربری.
* **اقدام اجرایی انجام‌شده:**
  1. طراحی کامپوننت پایه `ErrorStateView` در `src/components/common/ErrorStateView.tsx` با پشتیبانی از پیام‌های فارسی، اکشن «تلاش مجدد»، کپی شناسه خطا و دکمه بازگشت.
  2. ایجاد `SectionErrorBoundary` در `src/components/common/SectionErrorBoundary.tsx` با قابلیت ریست خودکار وضعیت بر مبنای `resetKeys` جهت ایزوله‌سازی تب‌ها و بخش‌های حساس بدون کرش کل صفحه.
  3. به‌روزرسانی و بومی‌سازی کامپوننت سراسری `ErrorBoundary.tsx`.
  4. اعمال `SectionErrorBoundary` در تب‌های صفحه حسابداری (`AccountingPage`) و صفحه پروژه‌ها (`ProjectsPage`).
  5. تجهیز صفحات گزارش‌های انبار و هشدار سفارش مجدد (`InventoryStatusPage` و `ReorderAlertsPage`) به `ErrorStateView`.
  6. پالایش و حذف واژگان لاتین درون پرانتز و بومی‌سازی اصطلاحات وضعیت‌ها در پروژه‌ها و پایش رویدادها.

---

## 📌 فاز ۷: شابلون ماژول‌های آتی، پایش پایداری و احیای CI/CD (Architecture Template, CI & Release)
**وضعیت:** ⏳ در حال اجرا (زیرفاز ۷.۱ تکمیل‌شده — v4.0.24)  
**تمرکز:** تضمین کیفیت پیوسته، تکمیل سوابق بدهی فنی و انتشار نسخه رسمی و پایدار

### زیرفاز ۷.۱: احیا و تثبیت پایپ‌لاین CI/CD در GitHub Actions (یافته T-1) — ✅ تکمیل‌شده (v4.0.24)
* **ریشه مسئله:** در ارزیابی مستقل T-1 مشخص شد که پایپ‌لاین CI به دلیل اشکالات پیکربندی محیط، عدم تفکیک استیج‌ها، نبود متغیرهای کامل تست و نیاز به کانتینر سرویس دیتابیس PostgreSQL نمی‌توانست تضمین کیفیت پیوسته را روی PRها و کامیت‌ها اعمال کند.
* **اقدام اجرایی انجام‌شده:**
  1. بازمهندسی کامل فایل `.github/workflows/ci.yml` با استانداردهای مدرن GitHub Actions v4.
  2. ایجاد استیج مستقل و سریع `lint-and-typecheck` جهت بررسی ایستای تایپ‌ها (`npm run lint`).
  3. استقرار کانتینر سرویس ایزوله `postgres:16-alpine` با Healthcheck فعال (`pg_isready`) جهت اجرای امن تست‌های پایگاه داده.
  4. تنظیم کامل متغیرهای محیطی آزمون (`DATABASE_URL`, `JWT_SECRET`, `ERP_SETUP_TOKEN`, `METRICS_TOKEN`, `ERP_ALLOW_TEST_CLEANUP`).
  5. استیج تفکیک‌شده `build` جهت بیلد کلاینت Vite و سرور CJS و آپلود آرتیفکت‌های توزیع (`dist/`).
  6. استیج اعتبارسنجی کانتینر داکر (`docker-gate`) و انتشار خودکار خلاصه نتایج آزمون در `GITHUB_STEP_SUMMARY`.
  7. حل قطعی بدهی فنی بحرانی TD-102 (یافته T-1).

### زیرفاز ۷.۲: همگام‌سازی ابزارهای آزمون و مدیریت وابستگی‌ها (یافته T-2) — ✅ تکمیل‌شده (v4.0.25)
* **ریشه مسئله:** در ارزیابی مستقل T-2 مشخص شد که وجود هم‌زمان دو لاک‌فایل (`bun.lock` و `package-lock.json`) و فقدان سنجه‌های تفکیک‌شده لایه‌ای، درصد اجرای کدهای واقعی و فرمت‌های استاندارد گزارش‌دهی CI در رانر آزمون‌ها، مانع از ارزیابی دقیق ماتریس تست‌ها می‌شد.
* **اقدام اجرایی انجام‌شده:**
  1. حذف قطعی `bun.lock` و تثبیت انحصاری `package-lock.json` به عنوان مرجع یگانه وابستگی‌های npm.
  2. ارتقای اسکریپت رانر آزمون‌ها (`scripts/run-tests.ts`) به پشتیبانی از فلگ‌های خط فرمان (`--layer=...`, `--json`, `--summary`, `--report-file=...`, `--verbose`).
  3. محاسبه و گزارش‌دهی دقیق سنجه‌های کیفیت شامل: درصد قبولی آزمون‌ها، درصد اجرای کدهای واقعی (Real-Code Execution Ratio)، میانگین زمان اجرای تست‌ها و تفکیک لایه‌ای.
  4. افزودن اسکریپت‌های شورت‌کات تفکیک‌شده در `package.json` شامل `test:unit`, `test:db`, `test:sec`, `test:api`, `test:reg`, `test:ci`.
  5. حل قطعی بدهی فنی TD-103 و یافته T-2 گزارش ارزیابی مستقل.

### زیرفاز ۷.۳: تعریف شابلون ماژول استاندارد توسعه‌های آتی (`src/modules/_template/`) — ✅ تکمیل‌شده (v4.0.26)
* **هدف و خروجی ملموس:** طراحی پکیج ماژولار و شابلون استاندارد برای توسعه سریع، پایدار و بدون خطای دامنه‌ها و ماژول‌های آینده در دایرکتوری `src/modules/_template/`.
* **محتویات و استانداردهای پیاده‌سازی‌شده:**
  1. `schema.ts`: اسکیما استاندارد Drizzle ORM با ستون‌های مقادیر پولی `numeric(..., { mode: 'number' })`، حذف منطقی `isDeleted`، نگارش `version` (جهت OCC) و ایندکس‌های بهینه.
  2. `types.ts`: تعاریف کامل TypeScript شامل اینترفیس‌های موجودیت، DTOهای ایجاد، ویرایش، فیلترهای صفحه‌بندی و وضعیت‌ها.
  3. `validation.ts`: اسکیماهای اعتبارسنجی زمان اجرا با Zod، تضمین نامنفی بودن مبالغ (`.nonnegative()`)، محدودسازی سقف صفحه‌بندی (`limit <= 100`) و اعتبارسنجی پارامترها و کوئری.
  4. `service.ts`: سرویس بیزینس لاجیک منطبق بر الگوی Read-Calculate-Update، قفل سطری `.for('update')`، شماره‌گذاری اتمیک، تراکنش‌های `tx`، حذف منطقی و ثبت خودکار لاگ ممیزی (`logActivity`).
  5. `routes.ts`: روت‌های اکسپرس کپسوله‌شده در `asyncHandler` با میدل‌ورهای `authenticateToken`، `authorize(...)`، `validate(...)` و محافظت ایدمپوتنسی (`idempotency`).
  6. `index.ts`: اکسپورت یکپارچه ساختار ماژول.
  7. `README.md`: راهنمای تفصیلی گام‌به‌گام به زبان فارسی برای دولوپرها و ایجنت‌های هوش مصنوعی جهت کلون و شخصی‌سازی ماژول‌های جدید.

### زیرفاز ۷.۴: به‌روزرسانی کامل `TECH_DEBT.md` و انتشار رسمی نگارش ۴ — ✅ تکمیل‌شده (v4.0.27)
* **هدف و خروجی ملموس:** اعتبارسنجی نهایی دفتر بدهی‌های فنی، اجرای کامل آزمون‌های رگرسیون، همگام‌سازی چنج‌لاگ‌ها و انتشار رسمی نقطه عطف نسخه ۴ سامانه ERP.
* **اقدامات انجام‌شده:**
  1. حسابرسی نهایی و بستن موفق تمامی ۱۷ بدهی فنی ناشی از گزارش ارزیابی مستقل (TD-088 تا TD-103) در `TECH_DEBT.md`.
  2. اجرای آزمون‌های یکپارچگی ماتریس ۲۱ سوییت و اعتبارسنجی صحت مایگریشن‌ها، تراکنش‌ها، لاگ‌های ممیزی و محاسبات پولی.
  3. به‌روزرسانی مستندات تاریخچه نگارش‌ها در `CHANGELOG.md` و ثبت رکورد رسمی انتشار در `src/data/changelogs/4.ts`.
  4. ارتقای نگارش سامانه به `v4.0.27` در `package.json` و تحقق ۱۰۰٪ اهداف نقشه راه جامع نگارش ۴.

---

## 🔗 جدول تطبیق یافته‌های ارزیابی مستقل با فازهای نقشه راه (Audit Traceability Matrix)

| کد یافته | شرح یافته در گزارش ارزیابی فنی | درجه اهمیت | فاز و زیرفاز متناظر در نقشه راه ۴ | وضعیت اجرایی |
| :---: | :--- | :---: | :---: | :---: |
| **S-1** | حفره CORS با Credentials در پروداکشن برای `*.run.app` | 🔴 **بحرانی** | زیرفاز ۲.۱ (فاز ۲: سنگربندی امنیتی) | ✅ **تکمیل‌شده (v4.0.20)** |
| **S-2** | بای‌پس گارد SSRF با آدرس‌های IPv4-mapped IPv6 | 🔴 **بحرانی** | زیرفاز ۲.۲ (فاز ۲: سنگربندی امنیتی) | ✅ **تکمیل‌شده (v4.0.21)** |
| **S-3** | دسترسی بدون اعتبارسنجی توکن به اندپوینت `/metrics` | 🟡 **مهم** | زیرفاز ۲.۳ (فاز ۲: سنگربندی امنیتی) | ✅ **تکمیل‌شده (v4.0.22)** |
| **S-4** | سکرت‌های پیش‌فرض هاردکد در سورس و اسکریپت راه‌اندازی | 🔴 **بحرانی** | زیرفاز ۲.۴ (فاز ۲: سنگربندی امنیتی) | ✅ **تکمیل‌شده (v4.0.22)** |
| **S-5** | افشای داده‌های بانکی پرسنل (PII) برای کاربران عمومی | 🔴 **بحرانی** | زیرفاز ۲.۵ (فاز ۲: سنگربندی امنیتی) | ✅ **تکمیل‌شده (v4.0.23)** |
| **S-6** | دور ریخته‌شدن نتیجه پارس Zod در میدل‌ور و پرمیشن‌های نادرست | 🟡 **مهم** | زیرفاز ۴.۱ (فاز ۴: قراردادهای زمان اجرا) | ✅ **تکمیل‌شده (v4.0.9)** |
| **F-1** | محاسبه دستمزد پرکیسی با محاسبات اعشاری شناور (Float) | 🔴 **بحرانی** | زیرفاز ۳.۲ (فاز ۳: صیانت مالی) | ✅ **تکمیل‌شده (v4.0.4)** |
| **F-2** | تولید غیرتراکنشی فیش حقوقی با ریسک پرداخت مضاعف | 🔴 **بحرانی** | زیرفاز ۳.۱ (فاز ۳: صیانت مالی) | ✅ **تکمیل‌شده (v4.0.4)** |
| **F-3** | بلعیده‌شدن خطاهای صدور سند حسابداری و لاگ‌های ممیزی | 🟡 **مهم** | زیرفاز ۳.۳ (فاز ۳: صیانت مالی) | ✅ **تکمیل‌شده (v4.0.5)** |
| **F-4** | شکنندگی کلید Idempotency و پوشش محدود در روت‌های پولی | 🟡 **مهم** | زیرفاز ۵.۱ (فاز ۵: ایدمپوتنسی و هم‌زمانی) | ✅ **تکمیل‌شده (v4.0.13)** |
| **D-1** | تضاد نوعی ستون‌های مالی در Drizzle و تبدیل مکرر `Number()` | 🟡 **مهم** | زیرفاز ۱.۴ (فاز ۱: اسکیما و دیتابیس) | ✅ **تکمیل‌شده (v4.0.7)** |
| **D-2** | عدم حفاظت از یکپارچگی ممیزی و حذف قطعی زودهنگام | 🟢 **بهبود** | زیرفاز ۱.۵ (فاز ۱: اسکیما و دیتابیس) | ✅ **تکمیل‌شده (v4.0.8)** |
| **A-1** | ابزارهای مرده مدیریت هم‌زمانی (`withOrderedLocks`) | 🟡 **مهم** | زیرفاز ۵.۲ (فاز ۵: ایدمپوتنسی و هم‌زمانی) | ✅ **تکمیل‌شده (v4.0.14)** |
| **A-2** | گلوگاه‌های پردازشی و کوئری‌های N+1 در گردش‌کار و احراز هویت | 🟡 **مهم** | زیرفاز ۵.۳ (فاز ۵: ایدمپوتنسی و هم‌زمانی) | ✅ **تکمیل‌شده (v4.0.15)** |
| **Q-1** | گود-کامپوننت‌های فرانت‌اند و محاسبه مالی در بدنه رندر | 🟡 **مهم** | زیرفاز ۶.۱ (فاز ۶: بهینه‌سازی کلاینت و UX) | ✅ **تکمیل‌شده (v4.0.16)** |
| **Q-2** | تکرار سیستماتیک کامپوننت‌های مودال و هندلرهای دستی | 🟡 **مهم** | زیرفاز ۶.۳ (فاز ۶: بهینه‌سازی کلاینت و UX) | ✅ **تکمیل‌شده (v4.0.18)** |
| **U-1** | جستجوی هر-کی‌استروک بدون Debounce و لیمیت‌های بزرگ | 🟢 **بهبود** | زیرفاز ۶.۲ (فاز ۶: بهینه‌سازی کلاینت و UX) | ✅ **تکمیل‌شده (v4.0.17)** |
| **T-1** | آسیب‌پذیری و قطع بودن پایپ‌لاین CI در GitHub Actions | 🔴 **بحرانی** | زیرفاز ۷.۱ (فاز ۷: تست و CI/CD) | ✅ **تکمیل‌شده (v4.0.24)** |
| **T-2** | فقدان سنجه پوشش آزمون، رانر دستی و دوگانگی لاک‌فایل‌ها | 🟢 **بهبود** | زیرفاز ۷.۲ (فاز ۷: تست و CI/CD) | ✅ **تکمیل‌شده (v4.0.25)** |

---

## 📊 ماتریس وضعیت و خروجی‌های ملموس فازهای نسخه ۴

| فاز | عنوان فاز | خروجی‌های ملموس و قابل اندازه‌گیری | اولویت اجرایی | وضعیت فعلی |
| :---: | :--- | :--- | :---: | :---: |
| **فاز ۱** | زیرساخت دامنه‌ای و رفع تضادهای نوعی دیتابیس | تکمیل تفکیک اسکیما/تایپ‌ها، رفع خطای ستون‌های عددی و تدوین قوانین | اولویت زیرساختی | ✅ **تکمیل‌شده** (v4.0.8) |
| **فاز ۲** | سنگربندی فوری امنیتی و کنترل سطح دسترسی | بستن CORS در پروداکشن، رفع SSRF، ایمن‌سازی متریک و ماسک PII پرسنل | 🔴 فوق‌العاده فوری | ✅ **تکمیل‌شده** (v4.0.23) |
| **فاز ۳** | صیانت از تراکنش‌های مالی، حقوق و دستمزد | اتمیک‌سازی صدور فیش، جایگزینی Float با `financialDecimal` و ارکستراسیون اسناد | 🔴 فوق‌العاده فوری | ✅ **تکمیل‌شده** (v4.0.6) |
| **فاز ۴** | سنگربندی قراردادهای زمان اجرا با Zod | اصلاح میدل‌ور ولیدیشن و اعمال قراردادهای ورودی اسناد، انبار و مالی | اولویت بالا | ✅ **تکمیل‌شده** (v4.0.12) |
| **فاز ۵** | ارتقای ایدمپوتنسی، هم‌زمانی و مقیاس‌پذیری | کلید سه‌گانه ایدمپوتنسی، رفع کدهای مرده هم‌زمانی و بهینه‌سازی کوئری | اولویت متوسط | ✅ **تکمیل‌شده** (v4.0.15) |
| **فاز ۶** | خلوت‌سازی، تفکیک گود-کامپوننت‌ها و UX | شکستن `BankAndTreasuryTab`، افزودن Debounce، ماژول‌های پایه و Error Boundary | اولویت متوسط | ✅ **تکمیل‌شده** (v4.0.19) |
| **فاز ۷** | شابلون ماژول‌ها، پایش پایداری و احیای CI/CD | احیای کامل CI، همگام‌سازی لاک‌فایل‌ها، تست‌های رگرسیون و انتشار رسمی | اولویت پایانی | ✅ **تکمیل‌شده** (v4.0.27) |

