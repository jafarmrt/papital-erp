# سند نقشه راه جامع ارتقای نسخه ۳ سامانه ERP (نسخه ۳.۰.۰)
> **Master Execution Roadmap for Version 3.0.0 — Financial Core & UI/UX Simplification**
> **تاریخ تدوین:** شهریور ۱۴۰۵ / September 2026  
> **تمرکز اصلی:** ارتقای هسته مالی، تسویه روان فاکتورها، سلامت دفاتر و **سادگی حداکثری و تمیزکاری رابط کاربری (UI/UX)**

---

## 🎯 اصول راهبردی حاکم بر توسعه نسخه ۳

این نقشه راه به صورت اختصاصی برای راهبری و اجرای گام‌به‌گام توسط **ایجنت هوش مصنوعی (AI Coding Agent)** طراحی شده است. هر فاز و زیرفاز مستقل، خودکفا و دارای معیارهای آزمون و اعتبارسنجی شفاف است.

### ۳ قانون طلایی طراحی و تجربه کاربری (UI/UX Mandates):
1. **اصل سادگی و خلوت‌سازی (Decluttering):**  
   حذف کلاسترها و ردیف‌های شلوغ دکمه‌ها. در هر جدول یا سطر، حداکثر ۱ تا ۲ اکشن اصلی و برجسته نمایش داده شود؛ سایر عملیات درون منوی کشویی سه‌نقطه («...») یا مودال‌های متمرکز قرار گیرند تا ذهن کاربر خسته و گیج نشود.
2. **فارسی‌سازی روان و حذف اصطلاحات غلیظ فنی و لاتین:**  
   ممنوعیت استفاده از عبارات انگلیسی و اختصارات فنی گنگ در بخش‌های کاربری (مانند SoD, Reversal, FSM, Void, Kardex, Draft Recovery, Batch, Matching Quality و ...). تمامی متون باید به زبان فارسی محترمانه، روشن و استاندارد بازار مالی ایران بازنویسی شوند.
3. **سازگاری و پایداری معماری (`AGENTS.md`):**  
   حفظ یکپارچگی تراکنش‌ها (`tx`)، ممنوعیت جهش داده با Raw SQL، عدم استفاده از `MAX() + 1` و اتکا به توالی‌های اتمیک PostgreSQL، و جلوگیری از تغییرات شکننده در ساختار پایدار پیشین.

---

## 🗺️ ساختار فازها و زیرفازهای نسخه ۳

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                       نقشه راه نسخه ۳.۰.۰ (V3 Roadmap)                     │
├─────────────────────────────────────────────────────────────────────────────┤
│ فاز ۱: تسویه سریع و آسان فاکتورها (Quick Invoice Settlement)                 │
│ فاز ۲: چرخه تایید مرحله‌ای و سلامت اسناد مالی (Financial Approval & Security)│
│ فاز ۳: صورت‌حساب جامع و گردش حساب تفصیلی اشخاص (Floating Detailed Ledger)    │
│ فاز ۴: تطبیق و مغایرت‌گیری روان بانکی (Simple Bank Reconciliation Engine)    │
│ فاز ۵: بازرس هوشمند سلامت مالی و دفاتر (Smart Financial Health Inspector)    │
│ فاز ۶: پیرایش، خلوت‌سازی و استانداردسازی رابط کاربری سراسر سامانه (Global UI) │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 📌 فاز ۱: تسویه سریع و آسان فاکتورها (Quick Invoice Settlement)

### هدف فاز:
امکان مشاهده شفاف وضعیت تسویه فاکتورها (تسویه‌نشده، تسویه جزئی، تسویه کامل) و ثبت مستقیم وصول/پرداخت وجه با ۱ کلیک از داخل لیست فاکتورها، بدون نیاز به خروج از صفحه یا سردرگمی میان صفحات مالی.

### زیرفاز ۱.۱: بک‌اند و محاسبه کارای وضعیت تسویه فاکتورها
* **شرح وظیفه:**
  * پیاده‌سازی متد `getInvoiceSettlementStatus` در بک‌اند با یک کوئری تجمیعی بهینه (`LEFT JOIN` روی `treasury_transactions.document_id`) بدون کوئری‌های تکراری N+1.
  * ارسال ۳ فیلد بهینه در پاسخ لیست فاکتورها:
    * `paidAmount`: جمع مبالغ پرداخت/دریافت شده تا این لحظه.
    * `remainingAmount`: مانده قابل پرداخت فاکتور.
    * `settlementStatus`: وضعیت تسویه شامل `unpaid` (تسویه‌نشده)، `partially_paid` (تسویه ناقص)، `fully_paid` (تسویه کامل).
* **فایل‌های هدف:**
  * `src/services/document.service.ts`
  * `src/routes/documents.routes.ts`
  * `src/types.ts`
* **معیار پذیرش (Definition of Done):**
  * فاکتورهای فروش و خرید در پاسخ API فیلدهای تسویه را به درستی و متناسب با تراکنش‌های ثبت‌شده دریافت کنند.

### زیرفاز ۱.۲: مودال سبک و کاربرپسند «تسویه فاکتور» (`InvoiceSettlementModal`)
* **شرح وظیفه:**
  * طراحی یک مودال متمرکز و خلوت با بارگذاری خودکار:
    * نام طرف‌حساب و شماره فاکتور
    * کل مبلغ فاکتور و مانده قابل تسویه (پیش‌فرض مبلغ روی مانده قرار گیرد)
    * انتخاب ساده روش پرداخت با ۴ کارت رادیویی بزرگ و آیکون‌دار: **کارت‌خوان (پوز)** | **واریز به حساب / کارت** | **وجه نقد** | **چک**
    * انتخاب حساب بانکی/صندوق مقصد
    * فیلد اختیاری شماره پیگیری و یادداشت کوتاه
  * ثبت امن از طریق اندپوینت استاندارد خزانه‌داری `/accounting/treasury` با شناسه `documentId` و صدور خودکار سند دوبل متناظر.
* **فایل‌های هدف:**
  * ایجاد کامپوننت جدید `src/components/invoices/InvoiceSettlementModal.tsx`
* **معیار پذیرش:**
  * با ثبت تسویه، تراکنش خزانه و سند حسابداری درجا صادر شده و پیام موفقیت روان نمایش داده شود.

### زیرفاز ۱.۳: بهبود جدول فاکتورها و خلوت‌سازی دکمه‌های ردیف
* **شرح وظیفه:**
  * افزودن برچسب وضعیت تسویه با رنگ‌های ملایم و خوانا:
    * سبز ملایم: «تسویه شده»
    * نارنجی ملایم: «بخشی پرداخت‌شده (مانده: ...)»
    * خاکستری ملایم: «تسویه‌نشده»
  * افزودن دکمه اصلی و برجسته «تسویه وجه» برای فاکتورهای دارای مانده.
  * ساماندهی سایر دکمه‌های پرشمار ردیف (گردش‌کار، جزییات، چاپ، ویرایش، حذف) در یک منوی مرتب بازشونده کشویی (`...`) برای جلوگیری از شلوغی و فشردگی افقی جدول.
* **فایل‌های هدف:**
  * `src/pages/InvoicesListPage.tsx`
* **معیار پذیرش:**
  * جدول فاکتورها تمیز و خوانا شده، دکمه‌های اضافه مخفی شوند و تسویه در کمتر از ۱۰ ثانیه با چند کلیک انجام شود.

---

## 📌 فاز ۲: چرخه تایید مرحله‌ای و سلامت اسناد مالی (Financial Approval & Security)

### هدف فاز:
ایجاد فرآیندی شفاف، امن و بدون پیچیدگی برای تایید اسناد مالی (پیش‌نویس ➔ تایید حسابدار ➔ نهایی و قطعی) تا اسناد به اشتباه یا بدون بررسی قطعی نشوند، با زبان کاملاً فارسی و قابل فهم.

### زیرفاز ۲.۱: تفکیک شفاف سه وضعیت اسناد حسابداری
* **شرح وظیفه:**
  * استانداردسازی وضعیت اسناد به ۳ حالت مشخص در UI و بک‌اند:
    1. **پیش‌نویس (یادداشت اولیه):** قابل ویرایش آزاد، بدون اثر در دفاتر قطعی.
    2. **تایید شده (حسابرسی‌شده):** اثرگذار در گزارش‌ها، اما با امکان برگشت به پیش‌نویس توسط سرپرست.
    3. **دائم و قطعی:** کاملاً قفل‌شده در دفاتر مالی، غیرقابل تغییر و غیرقابل حذف مستقیم؛ اصلاح تنها با صدور «سند اصلاحی» یا «سند برگشتی».
  * ارتقای روت‌های تایید و تغییر وضعیت در `src/routes/accounting.routes.ts` با اعمال لاگ حسابرسی دقیق و کنترل دسترسی.
* **فایل‌های هدف:**
  * `src/services/accounting/voucher.service.ts`
  * `src/routes/accounting.routes.ts`
  * `src/components/accounting/JournalVouchersTab.tsx`
* **معیار پذیرش:**
  * دکمه‌های تایید و تغییر وضعیت به صورت هوشمند بر اساس وضعیت سند و نقش کاربر نمایش یابند و مانع از حذف اسناد تاییدشده شوند.

### زیرفاز ۲.۲: ساده‌سازی و فارسی‌سازی عملیات اصلاح و ابطال سند
* **شرح وظیفه:**
  * جایگزینی اصطلاحات انگلیسی/فنی مانند `Reversal Voucher` یا `Void Action` با عبارات روشن:
    * «صدور سند برگشتی (ابطال سند)»
    * «صدور سند اصلاحی»
  * طراحی یک مودال کوچک و شسته‌رفته برای ثبت علت ابطال یا اصلاح (مثلاً: «خطا در مبلغ»، «ثبت فاکتور تکراری»، «اشتباه در حساب معین») و ایجاد خودکار سند معکوس با شماره پیگیری پیوسته.
* **فایل‌های هدف:**
  * `src/components/accounting/JournalVouchersTab.tsx`
* **معیار پذیرش:**
  * عملیات ابطال و اصلاح سند بدون نیاز به دانش عمیق نرم‌افزاری و با ۲ کلیک انجام شود و تراز دفاتر محفوظ بماند.

---

## 📌 فاز ۳: صورت‌حساب جامع و گردش حساب تفصیلی اشخاص (Floating Detailed Ledger)

### هدف فاز:
امکان مشاهده و چاپ صورت‌حساب شفاف مالی یک طرف‌حساب (مشتری، کارمند، یا تامین‌کننده) در کل سامانه، بدون درگیر شدن کاربر با ساختار درختی و کدهای پیچیده معین و کل.

### زیرفاز ۳.۱: سرویس استخراج ریزگردش تفصیلی فراگیر
* **شرح وظیفه:**
  * پیاده‌سازی متد `getDetailedPartyLedger` در `AccountingReportService`.
  * جستجوی سریع در تمام آرتیکل‌های اسناد حسابداری که فیلد `detailed_id` آن‌ها برابر با شخص انتخابی است.
  * محاسبه خودکار و دقیق:
    * مانده ابتدای دوره
    * جمع گردش بدهکار و جمع گردش بستانکار در بازه انتخابی
    * مانده لحظه‌ای پس از هر ردیف با تشخیص خودکار وضعیت (بدهکار / بستانکار / بی‌حساب).
* **فایل‌های هدف:**
  * `src/services/accounting/accountingReport.service.ts`
  * `src/routes/accounting.routes.ts`
* **معیار پذیرش:**
  * بازگشت سریع اطلاعات حتی برای مشتریان با صدها رکورد مالی بدون تاخیر.

### زیرفاز ۳.۲: صفحه خلوت و رسمی «صورت‌حساب طرف‌حساب»
* **شرح وظیفه:**
  * طراحی یک نمای بسیار مرتب در بخش گزارش‌های مالی:
    * جعبه جستجوی سریع نام شخص (مشتری/پرسنل/تامین‌کننده)
    * فیلتر بازه زمانی (از تاریخ تا تاریخ) با کلیدهای میان‌بر سریع (ماه جاری، ۳ ماه اخیر، سال جاری)
    * کارت خلاصه وضعیت در بالا: «وضعیت حساب: ۵,۰۰۰,۰۰۰ تومان بدهکار است» با رنگ شاخص
    * جدول خوانا با ستون‌های استاندارد: ردیف | تاریخ | شماره سند | شرح عملیات | بدهکار | بستانکار | مانده | نوع مانده
    * دکمه چاپ رسمی زیبا و خروجی اکسل با ۱ کلیک.
* **فایل‌های هدف:**
  * ایجاد `src/components/accounting/reports/PartyLedgerReportView.tsx`
  * یکپارچه‌سازی در `src/components/accounting/FinancialReportsTab.tsx`
* **معیار پذیرش:**
  * کاربر معمولی بدون تخصص حسابداری بتواند در چند ثانیه وضعیت کامل حساب هر شخص را بررسی و چاپ کند.

---

## 📌 فاز ۴: تطبیق و مغایرت‌گیری روان بانکی (Simple Bank Reconciliation Engine) [تکمیل شده در v2.9.2]

### هدف فاز:
ساده‌سازی فرآیند بررسی صورت‌حساب بانک با تراکنش‌های ثبت‌شده در سامانه، به گونه‌ای که هر کاربر بتواند فایل اکسل بانک را آپلود کرده و مغایرت‌ها را بدون فرمول‌های پیچیده حسابداری کشف و برطرف کند.

### زیرفاز ۴.۱: ساده‌سازی فرم دریافت صورت‌حساب و ارتقای تطبیق خودکار [x]
* **شرح وظیفه:**
  * جایگزینی فرم پیچیده کنونی با یک منطقه کشیدن و رها کردن فایل (Drag & Drop) بسیار خلوت و مدرن (`BankReconciliationModal.tsx`).
  * تشخیص هوشمند ستون‌های اکسل بانک‌های مختلف ایرانی (ملی، ملت، صادرات، سامان، پاسارگاد، تجارت، رسالت و ...) به صورت منعطف در `bankStatementMatcher.ts`.
  * الگوریتم تطبیق ۳ گانه:
    1. تطبیق بر اساس شماره پیگیری/ارجاع
    2. تطبیق بر اساس مبلغ دقیق و تاریخ یکسان یا روز قبل/بعد (تلورانس تاخیر پایا/ساتنا)
    3. نشانه‌گذاری اقلام مغایرت‌دار (واریزی یا برداشتی که در سیستم ثبت نشده).
* **فایل‌های پیاده‌سازی‌شده:**
  * `src/components/accounting/reconciliation/bankStatementMatcher.ts`
  * `src/components/accounting/reconciliation/BankReconciliationModal.tsx`
  * `src/components/accounting/BankAndTreasuryTab.tsx`
* **معیار پذیرش:**
  * پردازش سریع فایل بدون خطا و نمایش جدولی خلوت از نتایج با ۲ دسته مشخص: «اقلام تطبیق‌یافته» و «اقلام نیازمند اقدام». (محقق شد با موفقیت ۱۰۰٪ تست‌های خودکار)

### زیرفاز ۴.۲: ثبت آسان اقلام جاافتاده (کارمزد بانکی و واریزهای مستقیم) [x]
* **شرح وظیفه:**
  * برای ردیف‌هایی از صورت‌حساب که در سیستم وجود ندارند (مانند کارمزدهای بانکی ماهانه یا سود سپرده):
    * قرار دادن دکمه ساده «ثبت سریع به عنوان کارمزد بانک» (که با یک کلیک سند هزینه خدمات بانکی را صادر می‌کند).
    * دکمه «ثبت واریز به حساب» جهت تسویه نقدی سریع.
  * دکمه «چاپ صورت مغایرت بانکی» خلاصه و استاندارد.
* **فایل‌های هدف:**
  * `src/components/accounting/reconciliation/BankReconciliationModal.tsx`
* **معیار پذیرش:**
  * رفع مغایرت‌های بانکی یک ماه در کمتر از ۲ دقیقه بدون محاسبات دستی. (محقق شد)

---

## 📌 فاز ۵: بازرس هوشمند سلامت مالی و دفاتر (Smart Financial Health Inspector)

### هدف فاز:
تبدیل صفحه استاتیک چک‌لیست ممیزی به یک ابزار زنده، هوشمند و خودکار که با فشردن یک دکمه، کل پایگاه‌داده مالی و انبار را پایش کرده و مشکلات احتمالی را با راهکار رفع سریع (Quick Fix) نشان دهد.

### زیرفاز ۵.۱: موتور بک‌اند پایش سلامت دفاتر (`FinancialHealthCheckService`)
* **شرح وظیفه:**
  * پیاده‌سازی آزمون‌های خودکار به صورت بهینه و امن (Read-Only):
    1. **آزمون تراز اسناد:** بررسی اینکه آیا سندی در سیستم وجود دارد که جمع بدهکار و بستانکار آن برابر نباشد.
    2. **آزمون حساب‌های با مانده نامتعارف (خلاف ماهیت):** بررسی اینکه حسابی با ماهیت بدهکار (مانند صندوق یا بانک) مانده منفی/بستانکار پیدا نکرده باشد.
    3. **آزمون انطباق مالی و انبار:** مقایسه ارزش ریالی کاردکس موجودی کالاها با مانده حساب معین «موجودی کالا» در ترازنامه.
    4. **آزمون فاکتورهای معلق:** بررسی فاکتورهای پیش‌نویس رهاشده یا فاکتورهای نهایی بدون سند حسابداری.
    5. **آزمون چک‌های سررسیدگذشته بلاتکلیف:** استخراج چک‌هایی که تاریخ سررسید آن‌ها گذشته ولی تعیین تکلیف (وصول/برگشت) نشده‌اند.
  * ارائه پاسخ با ساختار مشخص: عنوان آزمون، وضعیت (`healthy` | `warning` | `error`)، پیام روان، تعداد موارد نیازمند بررسی، و شناسه رکوردهای دارای اشکال.
* **فایل‌های هدف:**
  * ایجاد `src/services/accounting/financialHealth.service.ts`
  * ثبت روت `/accounting/reports/health-check` در `src/routes/accounting.routes.ts`
* **معیار پذیرش:**
  * اجرای اسکن جامع در کمتر از ۵۰۰ میلی‌ثانیه بدون قفل کردن جداول یا کندی در دیتابیس.

### زیرفاز ۵.۲: رابط کاربری مدرن و تعاملی بازرس سلامت
* **شرح وظیفه:**
  * بازنویسی و مدرن‌سازی `AccountingAuditChecklistView.tsx`:
    * یک کارت بالای صفحه با دکمه جذاب «شروع اسکن سلامت دفاتر و حساب‌ها» و نمایش تاریخ آخرین اسکن.
    * امتیاز کلی سلامت سیستم (مثلاً: ۹۸٪ - وضعیت عالی و منظم).
    * کارت‌های مرتب برای هر حوزه با آیکون و نشانگرهای رنگی گویا (سبز برای سلامت، زرد برای توجه، قرمز برای خطا).
    * در صورت وجود خطا، دکمه مستقیم «مشاهده و رفع» برای انتقال مستقیم کاربر به سند یا فاکتور مربوطه.
* **فایل‌های هدف:**
  * `src/components/accounting/reports/AccountingAuditChecklistView.tsx`
* **معیار پذیرش:**
  * مدیر یا حسابدار بتواند در یک نگاه وضعیت انضباط مالی کل شرکت را بسنجد و در صورت بروز مغایرت سریعاً آن را تصحیح کند.

---

## 📌 فاز ۶: پیرایش، خلوت‌سازی و استانداردسازی رابط کاربری سراسر سامانه (Global UI Simplification & Decluttering)

### هدف فاز:
حل ریشه‌ای مشکل شلوغی صفحات، تجمع دکمه‌های گیج‌کننده و اصطلاحات نامانوس در تمامی ماژول‌های سامانه (فاکتورها، انبار، حسابداری، خزانه‌داری و تنظیمات) جهت خلق تجربه‌ای مدرن، آرامش‌بخش و فوق‌العاده ساده برای کاربر ایرانی.

### زیرفاز ۶.۱: استانداردسازی منوی عملیات سطرها و جداول (Action Menu Pattern)
* **شرح وظیفه:**
  * اصلاح جداول اصلی سیستم:
    * در هر ردیف جدول، حداکثر ۱ اکشن اولیه با رنگ ملایم (مانند «مشاهده» یا «ویرایش») قرار گیرد.
    * سایر عملیات (مانند «چاپ فاکتور»، «ابطال»، «گردش‌کار»، «تغییر وضعیت»، «کپی»، «حذف») درون یک منوی کشویی تمیز (Dropdown با آیکون سه‌نقطه `...`) جمع‌آوری شوند.
  * جلوگیری از فشرده‌شدن و خردشدن متن‌ها در ستون عملیات در نمایشگرهای مختلف.
* **فایل‌های هدف:**
  * `src/pages/InvoicesListPage.tsx`
  * `src/components/accounting/JournalVouchersTab.tsx`
  * `src/components/accounting/BankAndTreasuryTab.tsx`
  * `src/components/ItemsTab.tsx`
* **معیار پذیرش:**
  * کاهش حداقل ۶۰ درصدی تراکم دکمه‌ها در ردیف‌های جداول و یکدست شدن الگوی ناوبری.

### زیرفاز ۶.۲: پاکسازی اصطلاحات لاتین و واژگان بیش از حد فنی
* **شرح وظیفه:**
  * بازبینی و اصلاح برچسب‌ها، عنوان‌ها، تول‌تیپ‌ها و پیام‌های سیستم:
    * حذف عبارات لاتین و اختصارات نامفهوم مانند: `Lead`, `Kardex`, `Audit Trail`, `Approval Matrix`, `FSM`, `SLA`, `Method`, `Batch` و جایگزینی با معادل‌های طبیعی فارسی: «فرصت‌های فروش»، «کاردکس و گردش موجودی کالا»، «تاریخچه رویدادها»، «گردش کار تاییدات»، «روش تسویه»، «گروهی».
    * ساده‌سازی پیام‌های خطا و راهنماها به زبان فارسی صمیمی و حرفه‌ای (جلوگیری از نمایش خطاهای خام دیتابیس یا سرور به کاربر نهایی).
* **فایل‌های هدف:**
  * کامپوننت‌های فرانت‌اند در `src/components/accounting/`
  * کامپوننت‌های فرانت‌اند در `src/pages/`
  * ابزارهای کمکی `src/utils.ts`
* **معیار پذیرش:**
  * صفر شدن واژگان لاتین غیرضروری در رابط کاربری عمومی سامانه و افزایش رضایت و سادگی کاربری.

### زیرفاز ۶.۳: آزمون‌های یکپارچگی، اعتبارسنجی Build و ارتقای نسخه به 3.0.0
* **شرح وظیفه:**
  * اجرای کامل دستورات `npm run lint` و `npm run build` و رفع هرگونه اخطار تایپ‌اسکریپت یا استایل.
  * بروزرسانی نسخه به `3.0.0` در `package.json`.
  * ثبت کامل گزارش انتشار در چنجلاگ نسخه ۳ (`src/data/changelogs/v3.ts` یا ماژول‌های مربوطه و `CHANGELOG.md`).
* **فایل‌های هدف:**
  * `package.json`
  * `CHANGELOG.md`
  * `src/data/appInfoAndChangelog.ts`
* **معیار پذیرش:**
  * بیلد کامل و موفق بدون هیچ خطایی و آماده برای بهره‌برداری نهایی.

---

## 🚦 پروتکل اجرای ایجنت هوش مصنوعی (AI Execution Protocol)

برای حفظ نظم و تضمین بالاترین کیفیت در اجرای نقشه راه نسخه ۳، ایجنت موظف است:
1. **ترتیب زنجیره‌ای و مستقل فازها:** فازها دقیقاً به ترتیب از ۱ تا ۶ اجرا شوند و در هر نوبت، یک زیرفاز مشخص به اتمام برسد.
2. **بررسی سورس پیش از تغییر:** طبق اصول `AGENTS.md`، پیش از هر ویرایش محتوای فایل به طور کامل بررسی شود تا منطق‌های تثبیت‌شده قبلی حفظ شوند.
3. **تست و اعتبارسنجی در پایان هر زیرفاز:** پس از تکمیل هر زیرفاز، کامپایل برنامه سنجیده شده و خلاصه تغییرات به کاربر گزارش شود.
4. **توقف و تایید کاربر:** پس از پایان هر زیرفاز، ایجنت کار انجام‌شده را گزارش داده و منتظر تایید یا دستور کاربر برای گام بعدی بماند.
