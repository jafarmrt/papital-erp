/**
 * Central type definitions barrel file for ERP.
 * Modularized domain types are located in `./types/` according to V4 Architecture Roadmap.
 * Re-exports everything for 100% backwards compatibility.
 */
export * from './types/index';
